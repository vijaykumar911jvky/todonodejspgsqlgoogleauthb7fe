name: Trigger auto deployment for container-app-mnl2yrv44d7u6

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-mnl2yrv44d7u6-AutoDeployTrigger-732b65d5-1f7b-47cf-b2dd-1bcf98aebfe6.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPMNL2YRV44D7U6_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPMNL2YRV44D7U6_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPMNL2YRV44D7U6_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPMNL2YRV44D7U6_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPMNL2YRV44D7U6_REGISTRY_PASSWORD }}
          containerAppName: container-app-mnl2yrv44d7u6
          resourceGroup: todonodejspgsqlgoogleauthb7fea716
          imageToBuild: default/container-app-mnl2yrv44d7u6:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


